# ShellCheck 配置文件
# 基于 Bash 脚本编写心得.md 中的最佳实践

# 启用所有检查
enable=all

# 严重错误
severity=error

# 指定Bash版本
shell=bash

# 忽略的警告代码（根据需要调整）
# SC2034: 变量已声明但未使用（在某些情况下有用）
# SC2086: 参数扩展应加引号（项目中大部分已加引号）
# SC2154: 变量已引用但未声明（在set -u模式下会报错）

# 建议的警告代码
# SC2004: $在(( ))中是不必要的
# SC2010: 不应使用ls的输出来遍历
# SC2028: echo不支持转义序列（使用printf）
# SC2045: 遍历glob的结果应使用for in
# SC2046: 在命令替换中引用word拆分
# SC2066: 测试中的错误引号
# SC2076: 不要在没有glob时使用==
# SC2120: 函数在没有参数的情况下被调用
# SC2140: 单词中的ASCII拼写错误
# SC2153: 可能拼写错误/未赋值
# SC2164: 使用cd时可能丢失目录（使用cd || exit）
# SC2166: && 和 || 的优先级错误
# SC2196: egrep已弃用
# SC2207: 应该使用mapfile或read
# SC2222: 测试中的无效运算符
# SC2230: 不存在命令时使用which
# SC2248: printf中应使用-v

# 样式建议
# SC2002: cat的不必要使用
# SC2015: &&  || 的快捷方式
# SC2027: 不必要的引号
# SC2035: 使用.*或[...]时需要显式glob
# SC2044: 不应使用find的输出来遍历
# SC2086: 参数扩展应加引号（重要）
# SC2145: 混合的壳和执行路径